<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <disasterItem name="colorPrimary">@color/colorPrimary</disasterItem>
        <disasterItem name="colorPrimaryDark">@color/colorPrimaryDark</disasterItem>
        <disasterItem name="colorAccent">@color/colorAccent</disasterItem>
    </style>

    <style name="AppTheme.NoActionBar">
        <disasterItem name="windowActionBar">false</disasterItem>
        <disasterItem name="windowNoTitle">true</disasterItem>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
